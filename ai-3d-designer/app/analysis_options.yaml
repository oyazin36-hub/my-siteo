include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    # 未有効化のテンプレート。Firebase 有効化時にリネームして初めて解析対象になる。
    - "**/*.template"
  language:
    strict-casts: true
    strict-raw-types: true

linter:
  rules:
    - prefer_const_constructors
    - prefer_final_locals
    - avoid_print
